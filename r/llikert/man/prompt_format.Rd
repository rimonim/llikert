% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompt.R
\name{prompt_format}
\alias{prompt_format}
\title{Choose how a task is turned into a prompt}
\usage{
prompt_format(
  system = "{instructions}\\n\\n{scale}",
  user = "Text:\\n<text>\\n{item}\\n</text>\\n\\n{answer_instruction}",
  scale = "Response codes:\\n{codes}",
  code = "{response} = {label}",
  code_separator = "\\n"
)
}
\arguments{
\item{system}{Template for the system message, or \code{NULL} for no system
message.}

\item{user}{Template for the user message; must contain \code{{item}} once.}

\item{scale}{Template describing the response codes; must contain \code{{codes}}.}

\item{code}{Template for one category's line; must contain \code{{response}}.}

\item{code_separator}{Text placed between category lines.}
}
\value{
An object of class \code{llikert_prompt_format}.
}
\description{
A prompt format is a set of text templates. For every item, the model
receives these chat messages:
}
\details{
\enumerate{
\item a \strong{system} message built from \code{system} (left out when \code{system = NULL});
\item for each example: a \strong{user} message built from \code{user} with the example
item, followed by an \strong{assistant} message containing the example's
response code;
\item a \strong{user} message built from \code{user} with the item being scored.
}

The model's answer is then read at the start of its reply. Use
\code{\link[=task_messages]{task_messages()}} to see the messages for your own task; worked examples are
in \code{docs/prompts.md} in the package repository
(\url{https://github.com/rimonim/llikert/blob/main/docs/prompts.md}).
}
\section{Placeholders}{

Templates contain placeholders in curly braces, which are replaced by:
\itemize{
\item in \code{system} and \code{user}: \code{{instructions}} and \code{{answer_instruction}} (the
task's \code{instructions} and \code{answer_instruction}, from \code{\link[=scoring_task]{scoring_task()}}) and
\code{{scale}} (the filled-in \code{scale} template); \code{{item}} (the item being
scored) is allowed only in \code{user}, exactly once;
\item in \code{scale}: \code{{codes}}, the category lines joined by \code{code_separator};
\item in \code{code}, once per category: \code{{response}} (required), \code{{label}},
\code{{value}} (only when the task has values) and \code{{id}}.
}

Write \verb{\{\{} and \verb{\}\}} for literal braces. Placeholders are filled in a single
pass, so braces inside your instructions, labels or items are left as they
are.
}

\examples{
# the default: instructions and codes in the system message; the item and
# the answer instruction in the user message
prompt_format()

# instructions after the item, all in one user message
prompt_format(
  system = NULL,
  user = "{item}\n\n{instructions}\n{scale}\n{answer_instruction}"
)

# questionnaire items: the item alone, the scale on one line
prompt_format(
  system = "{instructions}\n{scale}",
  user = "{item}\n\n{answer_instruction}",
  scale = "{codes}",
  code = "{response} = {label}",
  code_separator = "; "
)
}
