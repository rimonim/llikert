% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{score_items}
\alias{score_items}
\title{Score items with a prepared task}
\usage{
score_items(
  items,
  ids = NULL,
  task,
  engine,
  chunk_size = 16L,
  checkpoint = NULL,
  resume = FALSE,
  progress = interactive(),
  force_unlock = FALSE
)
}
\arguments{
\item{items}{Character vector of items.}

\item{ids}{Optional unique ids (character, factor, or whole numbers); defaults
to row numbers.}

\item{task}{An \code{llikert_prepared} object from \code{\link[=prepare_task]{prepare_task()}}.}

\item{engine}{An \code{llikert_engine} from \code{\link[=scorer_connect]{scorer_connect()}}.}

\item{chunk_size}{Items per request (capped by the service limit).}

\item{checkpoint}{Optional folder for checkpoint files. It contains ids,
item hashes, and scores, never the items themselves or credentials.}

\item{resume}{Continue an existing checkpoint.}

\item{progress}{Show a progress bar.}

\item{force_unlock}{Remove a stale checkpoint lock left by a crashed session.}
}
\value{
An \code{llikert_result} tibble: \code{id}, \code{expected_value} (numeric tasks
only), and one probability column per category id. See
\code{\link[=llikert_result_diagnostics]{llikert_result_diagnostics()}} for the other quantities.
}
\description{
Sends items to the scoring service in chunks and returns one row per item,
in input order. An item is whatever fills \code{{item}} in the prompt: a text to
classify, a questionnaire statement, and so on. Missing (\code{NA}) and empty
items become diagnostic rows without being sent. With \code{checkpoint}, each
completed chunk is saved to that folder; after an interruption, run the same
call with \code{resume = TRUE} to score only the remaining items.
}
