% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{scorer_connect}
\alias{scorer_connect}
\title{Connect to a running scoring service}
\usage{
scorer_connect(
  url = Sys.getenv("LLIKERT_URL"),
  token = Sys.getenv("LLIKERT_TOKEN"),
  wait = 300,
  timeout = 300,
  scale_up_timeout = NULL,
  max_tries = 6L
)
}
\arguments{
\item{url}{Service URL. Defaults to the \code{LLIKERT_URL} environment variable.}

\item{token}{Bearer token, if the service requires one. Defaults to the
\code{LLIKERT_TOKEN} environment variable. Keep tokens out of scripts.}

\item{wait}{Seconds to wait for a starting service.}

\item{timeout}{Per-request timeout in seconds.}

\item{scale_up_timeout}{Optional seconds for a hosting proxy to hold requests
while scaling up (sent as \code{X-Scale-Up-Timeout}).}

\item{max_tries}{Maximum attempts for transient failures.}
}
\value{
An object of class \code{llikert_engine}.
}
\description{
Reads the service's identity from \verb{/v1/info}. While the service is starting
(for example a hosted endpoint scaling up from zero), polls for up to \code{wait}
seconds. Connecting never starts, stops, or downloads anything.
}
