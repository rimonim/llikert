% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{score_texts}
\alias{score_texts}
\title{Score texts with a prepared task}
\usage{
score_texts(
  texts,
  ids = NULL,
  task,
  engine,
  chunk_size = 16L,
  checkpoint = NULL,
  resume = FALSE,
  progress = interactive(),
  force_unlock = FALSE
)
}
\arguments{
\item{texts}{Character vector of texts.}

\item{ids}{Optional unique ids (character, factor, or whole numbers); defaults
to row numbers.}

\item{task}{An \code{llikert_prepared} object from \code{\link[=prepare_task]{prepare_task()}}.}

\item{engine}{An \code{llikert_engine} from \code{\link[=scorer_connect]{scorer_connect()}}.}

\item{chunk_size}{Items per request (capped by the service limit).}

\item{checkpoint}{Optional directory for checkpoint files. It contains ids,
text hashes, and scores, never texts or credentials.}

\item{resume}{Continue an existing checkpoint.}

\item{progress}{Show a progress bar.}

\item{force_unlock}{Remove a stale checkpoint lock left by a crashed session.}
}
\value{
An \code{llikert_result} tibble: \code{id}, \code{expected_value} (numeric tasks
only), and one probability column per category id. See
\code{\link[=llikert_result_diagnostics]{llikert_result_diagnostics()}} for the other quantities.
}
\description{
Sends texts to the scoring service in chunks and returns one row per input,
in input order. Missing (\code{NA}) and empty texts become diagnostic rows without
being sent. With \code{checkpoint}, each completed chunk is committed to that
directory; after an interruption, rerun with \code{resume = TRUE} to score only
the remaining items.
}
